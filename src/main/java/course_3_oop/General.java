package course_3_oop;

/**
 * This class should not be used because it beats the purpose of Abstraction
 * Person and Car are 2 different types (as in real life)
 * We need to create classes (blueprints) based on real entities
 */
public class General {

    // all of the properties
}
